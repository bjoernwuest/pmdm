Static content like CSS, images, fonts, icons, etc..

Except /static/public/ which is served to anyone, all other files are served only when the user must be authenticated (via login or in setup-route).