# Downstream Plan: PATT-008 — Request-bundling bypass for SSE sync documented as the single exception

## Upstream Change
Reference: `/report/Fix PATT-008 - Request-bundling bypass for SSE sync documented as the single exception.md`. Comments and folder documentation stating that the SSE expression sync is the single sanctioned mutation bypassing request bundling.

## Upstream's Own Assessment
"None — comments and folder documentation only."

## Applicability to This Project
Affected: No

Evidence:
- The documentation lives in shared files already merged (`src/ui/AGENTS.md`, `src/ui/api/sse_api.ts` comments).
- This project's own wrapper modules add no client mutations that bypass bundling.

## Target End State

## Approach

## Affected Scope

## Anticipated Manual Follow-Up

## Open Questions
