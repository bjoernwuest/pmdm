# Downstream Plan: VB-AI-003 — AGENTS.md canonical file examples all resolve

## Upstream Change
Reference: `/report/Fix VB-AI-003 - AGENTS.md canonical file examples all resolve.md`. Canonical file examples in the AGENTS.md files were corrected so all cited paths resolve.

## Upstream's Own Assessment
"None — documentation only."

## Applicability to This Project
Affected: No

Evidence:
- All `AGENTS.md` files in this tree (root and `src/**` subdirectories) are byte-identical to upstream's fixed versions (verified by the `src/` diff against `bun-starter` showing no AGENTS.md differences, and the root file being identical).
- This project's own design docs add no AGENTS.md file citations.

## Target End State

## Approach

## Affected Scope

## Anticipated Manual Follow-Up

## Open Questions
