# Downstream Plan: DOC-003 — Root AGENTS.md corrected on env, static mounts, TODO.md

## Upstream Change
Reference: `/report/Fix DOC-003 - Root AGENTS.md corrected on env, static mounts, TODO.md.md`. Root `AGENTS.md` was corrected on the env-variable table, the static mount descriptions, and the `TODO.md` status note.

## Upstream's Own Assessment
"None — documentation only."

## Applicability to This Project
Affected: No

Evidence:
- This project's root `AGENTS.md` is byte-identical to upstream's fixed version (diff against `bun-starter`: no differences) — it already contains the corrected env table, the `/public/*` mount entry, and the TODO.md note.
- The correction content is consistent with this project's own files (`.env` with explicit `ADVISORY_LOCK`; `README.md` updated separately under CFG-001).

## Target End State

## Approach

## Affected Scope

## Anticipated Manual Follow-Up

## Open Questions
