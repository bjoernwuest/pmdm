# Downstream Plan: DOC-004 — Folder AGENTS.md filename citations corrected

## Upstream Change
Reference: `/report/Fix DOC-004 - Folder AGENTS.md filename citations corrected.md`. Filename citations inside the folder-level `AGENTS.md` files were corrected to the actual file names.

## Upstream's Own Assessment
"None — documentation only."

## Applicability to This Project
Affected: No

Evidence:
- All folder-level `AGENTS.md` files under `src/` are byte-identical to upstream's fixed versions (the `src/` diff against `bun-starter` shows no `AGENTS.md` differences anywhere).
- This project's own layer docs add no filename citations beyond those already fixed upstream.

## Target End State

## Approach

## Affected Scope

## Anticipated Manual Follow-Up

## Open Questions
