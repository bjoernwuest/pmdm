# Downstream Plan: API-006 — OpenAPI security schemes named and completed

## Upstream Change
Reference: `/report/Fix API-006 - OpenAPI security schemes named and completed.md`. The OpenAPI document's security scheme id changed from `sessionId` to `apiKey`, plus new `sessionCookie`/`bearerToken` schemes, with per-route `security` references completed. Docs-facing only; no runtime contract change.

## Upstream's Own Assessment
"Yes — OpenAPI document content changed: scheme id `sessionId` → `apiKey` plus new `sessionCookie`/`bearerToken` schemes. Consumers that generate code against the old `sessionId` scheme id must adjust. Docs-facing only; no runtime contract change."

## Applicability to This Project
Affected: No

Evidence:
- Project-wide search for `sessionId` in `src/`, `scripts/`, `design/`, `static/`: the only hit is the anti-pattern note in the shared `src/api/AGENTS.md` ("Don't call `getSession(dbClient, sessionId)`"), which refers to a function parameter, not the OpenAPI scheme id.
- The OpenAPI document is generated by the shared `src/apps/api.ts` (merged; already carries the `apiKey`/`sessionCookie`/`bearerToken` schemes).
- This project does not generate client code from the OpenAPI spec: `scripts/generate_types.ts` generates TypeBox/Drizzle types only, and the UI wrappers are hand-written against the REST endpoints. No consumer of the old `sessionId` scheme id exists here.

## Target End State

## Approach

## Affected Scope

## Anticipated Manual Follow-Up

## Open Questions
